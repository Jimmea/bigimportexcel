<?php
/*
$objStorage = new Zend_Auth_Storage_Session(SESSION_AUTH_ADMIN);
$AppUI		= $objStorage->read();
if( !isset($AppUI->user_id) || !$AppUI->user_id )
{
	$this->_redirect(HOST_MANAGER . 'login');
} 
else 
{
	$this->view->iSlogin 	= 1;
	$this->view->AppUI 		= $AppUI;
	$this->AppUI			= $AppUI;
}

$controllerName	= $this->_request->getParam('controller');
$actionName		= $this->_request->getParam('action');
$this->controllerName			= $controllerName;
$this->actionName				= $actionName;
$this->view->controllerName		= $controllerName;
$this->view->actionName			= $actionName;

$MSGSUCCESS					= isset($_SESSION[MSGSUCCESS])?$_SESSION[MSGSUCCESS]:'';
if($MSGSUCCESS!='')
{
	unset($_SESSION[MSGSUCCESS]);
}
$this->view->MSGSUCCESS		= $MSGSUCCESS;
$MSGALERT					= isset($_SESSION[MSGALERT])?$_SESSION[MSGALERT]:'';
if($MSGALERT!='')
{
	unset($_SESSION[MSGALERT]);
}
$this->view->MSGALERT		= $MSGALERT;*/

?>